// igsr-core: Rust-side stub so the crate builds as a normal lib.
// The real logic lives in C (src/igsr.c, jitter.c, passes/*) and is
// compiled by build.rs via the `cc` crate. See include/igsr.h for the API.
